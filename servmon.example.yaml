# servmon 配置文件示例。复制为 servmon.yaml 放在启动目录，或用 -config 指定路径。
# 命令行里显式给出的参数会覆盖这里的同名配置。

# 监听地址
addr: ":8080"

# 访问令牌，为空则不鉴权（暴露到公网务必设置）
token: ""

# 允许控制的 systemd 服务；也可以写成逗号分隔的字符串 "nginx,docker"
services:
  - nginx
  - docker

# 采样间隔，Go duration 格式：2s、500ms、1m
interval: 2s
